const nomes = ["Fernanda", "Jõao", "Jorge", "Fabrício", "Felipe", "Julia", "Julho"]

export function Aleatório (Lista){
    const posição = Math.floor(Math.random()* lista.length);
    return Lista [posição];
}

export const nome = aleatório(nomes)
  
